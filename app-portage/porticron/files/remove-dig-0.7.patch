--- bin/porticron
+++ bin/porticron
@@ -115,7 +115,7 @@
 SCRIPT_NAME=$(basename $0)
 FQDN=$(hostname --fqdn)
 HOST=$(hostname -s)
-IP=$(dig +short ${FQDN} | tr '\n' ' ')
+IP=$(hostname -i)
 DATE=$(date -R)
 PORTDIR=$(portageq portdir)
 
