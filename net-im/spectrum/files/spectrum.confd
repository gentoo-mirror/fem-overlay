# /etc/conf.d/spectrum.PROTOCOL config file for spectrum PROTOCOL transport

# Start Transport when starting /etc/init.d/spectrum
AUTOSTART="yes"

# Configfile
CONFFILE="/etc/spectrum/PROTOCOL.cfg"

# PID
PIDFILE="/var/run/spectrum/PROTOCOL.pid"
