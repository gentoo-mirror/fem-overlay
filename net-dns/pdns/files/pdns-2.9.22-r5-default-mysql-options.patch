--- pdns-2.9.22.5/modules/gmysqlbackend/smysql.cc.orig	2012-01-11 10:56:20.314482521 +0100
+++ pdns-2.9.22.5/modules/gmysqlbackend/smysql.cc	2012-01-11 10:56:41.241147102 +0100
@@ -15,6 +15,9 @@
 	       const string &password)
 {
   mysql_init(&d_db);
+#if MYSQL_VERSION_ID > 32349
+  mysql_options(&d_db, MYSQL_READ_DEFAULT_GROUP, "client");
+#endif
   if (!mysql_real_connect(&d_db, host.empty() ? 0 : host.c_str(), 
 			  user.empty() ? 0 : user.c_str(), 
 			  password.empty() ? 0 : password.c_str(),
