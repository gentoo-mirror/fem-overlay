From: Marcel Pennewiss <gentoo@pennewiss.de>
Date: Tue Sep 11 11:02:00 CEST 2012
Subject: fix for paths in configure_file

backported patch from git-repository (86820022d1)

--- HomerBuild/CMakeConfig.txt
+++ HomerBuild/CMakeConfig.txt
@@ -88,7 +88,7 @@
 IF (DEFINED INSIDE_HOMER_BUILD)
 	INCLUDE (CheckIncludeFiles)
 	CONFIGURE_FILE(
-		${CMAKE_CURRENT_SOURCE_DIR}/../../../Homer-Conferencing/HomerBuild/resources/config.h.in 
+		${CMAKE_CURRENT_SOURCE_DIR}/../../HomerBuild/resources/config.h.in 
 		${CMAKE_CURRENT_BINARY_DIR}/config.h
 	)
 ENDIF()
@@ -98,8 +98,8 @@
 	INCLUDE (CheckIncludeFiles)
 	IF(APPLE)
 		CONFIGURE_FILE(
-			${CMAKE_CURRENT_SOURCE_DIR}/../../../Homer-Conferencing/HomerBuild/resources/osx/Info.plist.in 
+			${CMAKE_CURRENT_SOURCE_DIR}/../../HomerBuild/resources/osx/Info.plist.in 
 			${CMAKE_CURRENT_BINARY_DIR}/../../Info.plist
 		)
 	ENDIF()
-ENDIF()
\ No newline at end of file
+ENDIF()
